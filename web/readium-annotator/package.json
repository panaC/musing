{
  "name": "readium-annotator",
  "version": "1.0.0",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "tsc": "tsc",
    "build": "npm run tsc && npm run esbuild",
    "esbuild": "esbuild build/index.mjs --bundle --format=esm --target=esnext --outfile=build/index.js",
    "serve": "python3 -m http.server --protocol HTTP/1.1"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "description": "",
  "devDependencies": {
    "esbuild": "0.24.0",
    "typescript": "^5.6.3"
  },
  "dependencies": {
    "@apache-annotator/dom": "^0.2.0",
    "@apache-annotator/selector": "^0.2.0",
    "@medv/finder": "^3.2.0"
  }
}
